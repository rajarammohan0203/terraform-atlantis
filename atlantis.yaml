version: 3
projects:
  - dir: s3-demo
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
  - dir: ecs-atlantis-deploy
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
